    // Check if the response message is "Invalid JSON format from Mainframe"
      if (response.data.message === "Invalid JSON format from Mainframe") {
        throw new Error("Invalid JSON format from Mainframe");
      }
