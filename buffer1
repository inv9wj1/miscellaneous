import { Modal } from 'antd';

const modalResponse = (
  <Modal
    title="Alert!"
    visible={visibleModalResponse}
    onCancel={() => setVisibleModalResponse(false)}
    footer={null}
  >
    <p>Data is saved..</p>
  </Modal>
);
