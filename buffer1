 try {
          JSON.parse(responseStr);
          res.status(200).json(JSON.parse(responseStr));
        } catch (error) {
          console.log(error);
          res
            .status(200)
            .json({ message: "Invalid JSON format from Mainframe" });
        }
