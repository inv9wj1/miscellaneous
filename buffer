import { Modal, Button, Input } from 'antd';

const { TextArea } = Input;

const modalFileEditor = (
  <Modal
    title="File Editor"
    visible={visible}
    onCancel={() => setVisible(false)}
    footer={[
      <Button key="cancel" onClick={() => setVisible(false)}>
        Cancel
      </Button>,
      <Button key="save" type="primary" onClick={handleOnSaveButtonClick}>
        Save
      </Button>
    ]}
  >
    <TextArea
      placeholder="Data will be shown once it's loaded..."
      id="myTextArea"
      rows={80}
      value={fileContent}
      onChange={(e) => handleDataChange(e)}
    />
  </Modal>
);
