 // Alert if the fileName matches
    if (fileName === fileName1) {
      alert(`File "${fileName}" is already open in modal 1`);
    } else if (fileName === fileName2) {
      alert(`File "${fileName}" is already open in modal 2`);
    } else if (fileName === fileName3) {
      alert(`File "${fileName}" is already open in modal 3`);
    } else if (fileName === fileName4) {
      alert(`File "${fileName}" is already open in modal 4`);
    } else if (fileName === fileName5) {
      alert(`File "${fileName}" is already open in modal 5`);
    }
